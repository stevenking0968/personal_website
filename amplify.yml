version: 1
backend:
  phases:
    build:
      commands:
        - npm run server
frontend:
  phases:
    preBuild:
      commands:
        - cd client
    build:
      commands:
        - npm run client
  artifacts:
    files:
        - "**/*"
    baseDirectory: build
cache:
  paths:
      - node_modules/**/*