version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --verbose
    build:
      commands:
        - npm run dev
  artifacts:
    files:
        - "**/*"
    baseDirectory: build
cache:
  paths:
      - node_modules/**/*