version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client
        - npm install --verbose
    build:
      commands:
        - npm start
  artifacts:
    files:
        - "**/*"
    baseDirectory: build
cache:
  paths:
      - node_modules/**/*