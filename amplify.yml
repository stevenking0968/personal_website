version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --verbose
    build:
      commands:
        - cd client
        - npm start
  artifacts:
    files:
        - "**/*"
    baseDirectory: build
cache:
  paths:
      - node_modules/**/*