version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client
        - npm install --verbose
    build:
      commands:
        - npm start
  artifacts:
    files:
        - "**/*"
    baseDirectory: client/build
cache:
  paths:
      - client/node_modules/**/*